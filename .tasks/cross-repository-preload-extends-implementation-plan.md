- [ ] Refactor `loadConfiguration` in `index.ts` to return ordered project scopes containing a project root, file patterns, and context names; keep bare `extends` values as packaged presets merged into the owning project scope, resolve `./` and `../` values from the declaring `AGENTS.yml` as project directories, load and validate each referenced `AGENTS.yml` `pi-preload` section, allow its relative file patterns to use its project root, recurse into its project references, and reject cycles with resolved `AGENTS.yml` paths.
- [ ] Change file collection to run `globby` separately for each project scope with that project root as `cwd`, so the parent repository’s `.gitignore` cannot exclude an explicitly referenced project and each referenced project still applies its own ignore rules, negative patterns, standard exclusions, explicit binary handling, and symbolic-link policy.
- [ ] Carry the owning project root with each context name, invoke the context facts loader with that root as `cwd`, and deduplicate context loads by project root and context name while retaining inherited-before-local order.
- [ ] Merge the scoped file candidates by resolved absolute path, label each file relative to the original session directory, apply the existing deterministic sort and global file and byte limits to the merged result, and write the merged blocks only to the original session directory’s `PRELOAD.md`.
- [ ] Update `test/unit.test.ts` to prove that existing preset inheritance is unchanged, a parent configuration can extend a child directory excluded by the parent `.gitignore`, child file patterns and context facts use the child root, nested project references load, cycles fail, and the existing global limits apply across project scopes.
