- [ ] Define the path-aware `extends` contract in `agents.ts` and `.pi/schemas/AGENTS.schema.json` without changing its string-array shape: keep bare values as packaged preset names, treat values that start with `./` or `../` or are absolute as project directory references, resolve relative references from the declaring `AGENTS.yml`, and load `AGENTS.yml` from the referenced directory.
- [ ] Replace the flat configuration result in `index.ts` with ordered source scopes that retain the configuration path, project root, file patterns, and context names, while preserving current preset inheritance and inherited-before-local ordering.
- [ ] Extend recursive configuration loading so project references read and validate the referenced top-level `pi-preload` section, allow that project’s relative file patterns to use its own root, keep existing preset rules unchanged, and use canonical configuration paths for duplicate and cycle detection with clear reference-chain errors.
- [ ] Enumerate each source scope with its project root as the `globby` working directory so an explicit project reference is not excluded by the parent repository’s `.gitignore`, while the referenced project’s own ignore rules, negative patterns, standard exclusions, explicit binary selection, and symbolic-link behavior remain unchanged.
- [ ] Load each context with its owning project root as `cwd`, deduplicate contexts by canonical project root and context name, and preserve the current context rendering and order.
- [ ] Merge all scope results by canonical file path, deduplicate repeated files, label files relative to the original session directory, sort them deterministically, apply the existing per-file and combined limits to the merged preload, and write only the original session project’s `PRELOAD.md` with the combined file count and bytes.
- [ ] Update `test/unit.test.ts` with focused coverage for unchanged preset behavior, a parent that extends a git-ignored child whose relative files are included, referenced-context `cwd`, duplicate suppression, missing referenced configuration, nested references, and cycle rejection.
- [ ] Run the focused unit tests and the existing end-to-end suite, then run type checking and Biome, a clean full install, a clean production install, and a production extension-load check without provider credentials.
