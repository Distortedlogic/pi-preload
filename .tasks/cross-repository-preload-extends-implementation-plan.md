- [ ] Extend the existing `loadConfiguration` recursion in `index.ts` to return ordered project scopes containing a project root, file patterns, and context names; reuse `loadYamlConfiguration` and the existing file-size and TypeBox validation for referenced `AGENTS.yml` files, reuse `pMap` with `CONCURRENCY` and the active signal for `extends`, keep bare values as packaged presets merged into the owning project scope, resolve `./` and `../` values with `dirname` and `resolve` as project directories, allow relative patterns from each referenced project root, and reject cycles with the existing resolved-path ancestor stack.
- [ ] Change file collection to run `globby` separately for each project scope with that project root as `cwd`, so the parent repository’s `.gitignore` cannot exclude an explicitly referenced project and each referenced project still applies its own ignore rules, negative patterns, standard exclusions, explicit binary handling, and symbolic-link policy.
- [ ] Change `loadContextSources` to accept ordered project-root and context-name pairs, then reuse `validateContextName`, `loadContextSource`, and the existing Nunjucks environment to invoke each facts loader with its owning project root as `cwd` and deduplicate only equal root-and-name pairs.
- [ ] Merge scoped candidates with a `Map` keyed by `resolve(scope.root, file.path)`, use `relative` for labels from the original session directory, and reuse the existing sort, byte accounting, limit errors, block serialization, and single root `PRELOAD.md` write for the merged result.
- [ ] Update `test/unit.test.ts` to prove that existing preset inheritance is unchanged, a parent configuration can extend a child directory excluded by the parent `.gitignore`, child file patterns and context facts use the child root, nested project references load, cycles fail, and the existing global limits apply across project scopes.
