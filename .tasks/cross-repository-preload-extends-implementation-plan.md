- [ ] Define the cross-repository `extends` contract in `agents.ts` and `.pi/schemas/AGENTS.schema.json`: keep bare names as packaged preset names, treat values that start with `./` or `../` or are absolute paths as project directory references, resolve relative references from the directory of the declaring `AGENTS.yml`, and map each project directory reference to its `AGENTS.yml`.
- [ ] Replace the flat preload configuration result in `index.ts` with a source-scoped plan that retains each configuration path, project root, file patterns, context names, and declaration order, while keeping the current preset inheritance behavior and applying inherited entries before local entries.
- [ ] Refactor configuration loading so a preset extends the effective configuration of its owning project, but a project path loads the top-level `pi-preload` section from the referenced `AGENTS.yml` as a separate scope whose relative file patterns use that referenced project root.
- [ ] Add strict recursive project-reference validation with regular-file and size checks, clear errors for a missing `AGENTS.yml` or missing `pi-preload` section, canonical real paths for identity, and cycle detection that reports the reference chain, including cycles through symlinks or nested project references.
- [ ] Enumerate files once per project scope with that scope as the `globby` working directory so an explicit project reference overrides the parent repository ignore boundary, while the referenced project’s own ignore rules, negative patterns, standard preload exclusions, explicit binary-file selection, and symbolic-link policy continue to apply.
- [ ] Track each selected file by canonical absolute path, deduplicate files across presets and project references before reading them, derive stable display paths relative to the original session directory, and preserve deterministic sorting when files come from more than one project root.
- [ ] Associate every configured context with its owning project root, run each context facts loader with that root as `cwd`, deduplicate contexts by canonical project root and context name, and preserve the current rendered context format and inherited-before-local order.
- [ ] Apply `MAX_FILES`, per-file byte limits, selected-file byte limits, rendered-context byte limits, cancellation checks, and the final `PRELOAD.md` write to the combined preload result, while writing only the original session project’s snapshot and reporting the combined file count and bytes.
- [ ] Update the existing unit tests in `test/unit.test.ts` for path classification, child-relative globs, a parent `.gitignore` that excludes an explicitly referenced child project, nested project references, preset compatibility, duplicate files, source-scoped contexts, missing referenced configuration, and canonical cycle detection.
- [ ] Update the existing end-to-end tests in `test/e2e.test.ts` with a trusted parent project that extends a git-ignored child project and verify that the emitted hidden preload message and root `PRELOAD.md` contain the child project content without creating a child snapshot.
- [ ] Run the targeted unit and end-to-end tests, then run type checking and Biome, a clean full install, a clean production install, and a production extension-load check without provider credentials to confirm that the packaged extension still loads and the new path behavior has no dependency or startup regressions.
