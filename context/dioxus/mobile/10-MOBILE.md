# Mobile Runtime

- Keep UI, state, and application logic in shared Rust. Put native calls behind `target_os` guards.
- Declare Dioxus-supported permissions in `Dioxus.toml`; the Dioxus CLI maps them to native platform metadata.
